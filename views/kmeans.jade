//
   Created by Luan on 15. 4. 7..

extends layout
block content
    div.container
        div.row
            h2 Kmeans
            legend
            form(method='post', action='/kmeans')
                div
                    input(type='text', name='body', id='data', style='width:100%;height:300px;')
                button.btn-info(type='submit', value='Send') Test
            script.
                $(function() {
                    alert('in');
                    $("#data").on("keyup", function(e){
                        $("#keyCode").html(e.which);
                            if(e.which==13)
                                {
                                    alert("enter Key");
                                }
                        });
                });
